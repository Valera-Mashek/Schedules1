<Page x:Class="WpfApp.AutorizationPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp"
      mc:Ignorable="d" 
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      d:DesignHeight="450" d:DesignWidth="800"
      Title="AutorizationPage">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="20"/>
            <RowDefinition Height="40"/>
            <RowDefinition Height="40"/>
            <RowDefinition Height="100"/>
        </Grid.RowDefinitions>
        <Label Content="Имя пользователя" Grid.Row="1" VerticalAlignment="Top" HorizontalAlignment="Center"/>
        <TextBox Name="LoginBox" Height="25" Width="200" Grid.Row="1" Margin="0 0 0 -5"  VerticalAlignment="Bottom" />
        <Label Content="Пароль:" Grid.Row="2" VerticalAlignment="Top" HorizontalAlignment="Center"/>
        <PasswordBox Name="PasswordBox" Height="25" Width="200" Grid.Row="2" VerticalAlignment="Bottom"/>
        <Button Background="#4f666a" Name="btnSignIn" Grid.Row="3" Width="175" Height="30" Content="Войти" Click="btnSignIn_Click" Margin="312,0,312,61" VerticalAlignment="Bottom" />
    </Grid>
</Page>
